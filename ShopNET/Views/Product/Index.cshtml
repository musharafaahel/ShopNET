@model List<ShopNET.Models.Product>

@{
    ViewData["Title"] = "Our Products";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Our Products</h2>
    <span class="text-muted">@Model.Count products found</span>
</div>

<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var product in Model)
    {
        <div class="col">
            @* <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">@product.Name</h5>
                    <p class="card-text text-muted">@product.Description</p>
                    <p class="card-text">
                        <span class="fs-4 fw-bold text-success">$@product.Price</span>
                    </p>
                    <p class="card-text">
                        <small class="text-muted">Stock: @product.Stock units</small>
                    </p>
                </div>
                <div class="card-footer bg-white border-0 pb-3">
                    <a href="/Product/Details/@product.Id" class="btn btn-dark w-100">View Details</a>
                </div>
            </div> *@
            @await Html.PartialAsync("_ProductCard", product)
        </div>
    }
</div>